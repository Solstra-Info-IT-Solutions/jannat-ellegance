'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { ChevronLeft, ChevronRight, Search } from 'lucide-react';
import AdminShell from '@/components/admin/AdminShell';
type Order = { id: string; customerName: string; customerEmail: string; createdAt: string; items: unknown[]; total: number; paymentStatus: string; status: string; shippingInfo?: { courierName?: string; trackingNumber?: string } };
const statuses = ['', 'confirmed', 'processing', 'packed', 'shipped', 'in_transit', 'out_for_delivery', 'delivered', 'cancelled']; const label = (value: string) => value.replace(/_/g, ' ').replace(/\b\w/g, (character) => character.toUpperCase());
export default function AdminOrdersPage() { const [orders, setOrders] = useState<Order[]>([]); const [search, setSearch] = useState(''); const [status, setStatus] = useState(''); const [from, setFrom] = useState(''); const [to, setTo] = useState(''); const [page, setPage] = useState(1); const [pages, setPages] = useState(1); const [error, setError] = useState(''); const load = () => fetch(`/api/admin/orders?page=${page}&search=${encodeURIComponent(search)}&status=${status}&from=${from}&to=${to}`, { credentials: 'include' }).then((response) => response.ok ? response.json() : null).then((data) => { if (!data?.success) throw new Error(); setOrders(data.orders); setPages(data.pages); }).catch(() => setError('Unable to load orders.')); useEffect(() => { const timer = setTimeout(load, 250); return () => clearTimeout(timer); }, [page, search, status, from, to]); return <AdminShell title="Order Management"><div className="mb-5 grid gap-3 md:grid-cols-5"><div className="relative md:col-span-2"><Search size={16} className="absolute left-4 top-1/2 -translate-y-1/2 text-maroon-700" /><input value={search} onChange={(event) => { setSearch(event.target.value); setPage(1); }} placeholder="Customer, email or order ID" className="w-full rounded-full border border-maroon-100 bg-white py-3 pl-10 pr-4 text-sm" /></div><select value={status} onChange={(event) => { setStatus(event.target.value); setPage(1); }} className="rounded-full border border-maroon-100 bg-white px-4 text-sm">{statuses.map((item) => <option key={item} value={item}>{item ? label(item) : 'All statuses'}</option>)}</select><input type="date" value={from} onChange={(event) => { setFrom(event.target.value); setPage(1); }} className="rounded-full border border-maroon-100 bg-white px-4 text-sm" /><input type="date" value={to} onChange={(event) => { setTo(event.target.value); setPage(1); }} className="rounded-full border border-maroon-100 bg-white px-4 text-sm" /></div>{error && <p className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="overflow-x-auto rounded-3xl border border-maroon-100 bg-white"><table className="w-full min-w-[1050px] text-left text-sm"><thead className="bg-maroon-50 text-xs uppercase text-maroon-900"><tr><th className="p-4">Order</th><th className="p-4">Customer</th><th className="p-4">Amount</th><th className="p-4">Payment</th><th className="p-4">Order status</th><th className="p-4">Courier / Tracking</th><th className="p-4">Date</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id} className="border-t border-maroon-50"><td className="p-4"><Link href={`/admin/orders/${order.id}`} className="font-mono font-bold text-maroon-950">JE{order.id.slice(-8).toUpperCase()}</Link></td><td className="p-4"><p className="font-semibold">{order.customerName}</p><p className="text-xs text-gray-500">{order.customerEmail}</p></td><td className="p-4 font-bold">₹{order.total.toLocaleString('en-IN')}</td><td className="p-4 capitalize">{order.paymentStatus}</td><td className="p-4"><span className="rounded-full bg-maroon-50 px-3 py-1 text-xs font-bold text-maroon-800">{label(order.status)}</span></td><td className="p-4 text-xs text-gray-600">{order.shippingInfo?.courierName || '—'}<br />{order.shippingInfo?.trackingNumber || '—'}</td><td className="p-4 text-gray-600">{new Date(order.createdAt).toLocaleDateString('en-IN')}</td></tr>)}</tbody></table>{!orders.length && <p className="p-10 text-center text-sm text-gray-500">No orders match these filters.</p>}</div><div className="mt-5 flex items-center justify-between"><p className="text-sm text-gray-500">Page {page} of {pages}</p><div className="flex gap-2"><button disabled={page <= 1} onClick={() => setPage(page - 1)} className="rounded-full border border-maroon-100 p-2 disabled:opacity-40"><ChevronLeft size={16} /></button><button disabled={page >= pages} onClick={() => setPage(page + 1)} className="rounded-full border border-maroon-100 p-2 disabled:opacity-40"><ChevronRight size={16} /></button></div></div></AdminShell>; }
