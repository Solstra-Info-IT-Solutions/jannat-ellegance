'use client';

import { useEffect, useState } from 'react';
import Image from 'next/image';
import Link from 'next/link';
import { ArrowLeft, Heart, Minus, Plus, ShoppingBag } from 'lucide-react';
import { useRouter } from 'next/navigation';
import { useCart } from '@/context/CartContext';
import { Product } from '@/types';

const salePrice = (product: Product) => product.salePrice ?? (product.isOnSale ? Math.max(0, product.price - (product.discountType === 'percentage' ? product.price * product.discount / 100 : product.discount)) : product.price);
export default function ProductPage({ params }: { params: { id: string } }) {
  const router = useRouter(); const { addToCart, toggleWishlist, isWishlisted } = useCart();
  const [product, setProduct] = useState<Product | null>(null); const [loading, setLoading] = useState(true); const [selectedSize, setSelectedSize] = useState(''); const [quantity, setQuantity] = useState(1); const [imageIndex, setImageIndex] = useState(0);
  useEffect(() => { let active = true; fetch(`/api/products/${params.id}`).then((res) => res.ok ? res.json() : null).then((data) => { if (!active) return; setProduct(data?.product || null); setSelectedSize(data?.product?.sizes?.find((item: { stock: number }) => item.stock > 0)?.size || ''); }).catch(() => active && setProduct(null)).finally(() => active && setLoading(false)); return () => { active = false; }; }, [params.id]);
  if (loading) return <main className="min-h-[70vh] grid place-items-center bg-[#fff8fa] text-maroon-900">Loading design…</main>;
  if (!product) return <main className="min-h-[70vh] grid place-items-center bg-[#fff8fa] text-center"><div><h1 className="font-serif text-3xl text-maroon-950">Design not found</h1><Link href="/shop" className="inline-block mt-5 rounded-full bg-maroon-800 px-6 py-3 text-xs font-bold uppercase tracking-wider text-white">Return to shop</Link></div></main>;
  const imageUrls = product.imageUrls.length ? product.imageUrls : ['/images/logo.jpeg']; const selectedStock = product.sizes.find((item) => item.size === selectedSize)?.stock || 0; const price = salePrice(product);
  const add = () => { if (!selectedSize || !selectedStock) return; addToCart(product, selectedSize, quantity); };
  return <main className="min-h-screen bg-[#fff8fa] py-8 sm:py-12"><div className="mx-auto max-w-7xl px-4 sm:px-6"><button onClick={() => router.back()} className="mb-8 flex items-center gap-2 text-sm font-semibold text-maroon-800"><ArrowLeft size={16} />Back to shop</button><div className="grid gap-10 lg:grid-cols-2"><section><div className="relative aspect-[4/5] overflow-hidden rounded-[30px] border border-maroon-100 bg-white shadow-luxury"><Image src={imageUrls[imageIndex]} alt={product.name} fill priority className="object-cover object-top" /><button onClick={() => toggleWishlist(product)} aria-label="Save to wishlist" className="absolute right-5 top-5 grid h-11 w-11 place-items-center rounded-full bg-white/90 text-maroon-800 shadow"><Heart size={19} className={isWishlisted(product.id) ? 'fill-current' : ''} /></button></div>{imageUrls.length > 1 && <div className="mt-4 flex gap-3 overflow-auto">{imageUrls.map((url, index) => <button key={url} onClick={() => setImageIndex(index)} className={`relative h-20 w-16 flex-none overflow-hidden rounded-xl border-2 ${index === imageIndex ? 'border-maroon-800' : 'border-transparent'}`}><Image src={url} alt="Product view" fill className="object-cover" /></button>)}</div>}</section><section className="font-sans"><p className="inline-block rounded-full border border-maroon-100 bg-white px-4 py-1.5 text-xs font-bold uppercase tracking-wider text-maroon-800">{product.category}</p><h1 className="mt-4 font-serif text-4xl text-maroon-950">{product.name}</h1><div className="mt-6 flex items-center gap-3"><span className="text-3xl font-bold text-maroon-800">₹{price.toLocaleString('en-IN')}</span>{product.isOnSale && <><span className="text-lg text-gray-400 line-through">₹{product.price.toLocaleString('en-IN')}</span><span className="rounded bg-green-100 px-2 py-1 text-xs font-bold text-green-700">SALE</span></>}</div><p className="mt-6 leading-7 text-gray-600">{product.description}</p><div className="my-7 h-px bg-maroon-100" /><p className="mb-3 text-sm font-bold text-maroon-950">Select size</p><div className="flex flex-wrap gap-2">{product.sizes.map((item) => <button disabled={item.stock < 1} key={item.size} onClick={() => { setSelectedSize(item.size); setQuantity(1); }} className={`h-11 min-w-11 rounded-full border px-3 text-xs font-bold ${selectedSize === item.size ? 'border-maroon-850 bg-maroon-850 text-white' : 'border-maroon-100 bg-white text-maroon-950'} disabled:cursor-not-allowed disabled:opacity-40`}>{item.size}</button>)}</div>{selectedSize && <p className="mt-2 text-xs text-gray-500">{selectedStock} available</p>}<div className="mt-7 flex items-center gap-4"><div className="flex items-center rounded-full border border-maroon-200 bg-white"><button onClick={() => setQuantity(Math.max(1, quantity - 1))} className="p-3"><Minus size={15} /></button><span className="w-8 text-center text-sm font-bold">{quantity}</span><button disabled={quantity >= selectedStock} onClick={() => setQuantity(quantity + 1)} className="p-3 disabled:opacity-40"><Plus size={15} /></button></div></div><div className="mt-7 grid gap-3 sm:grid-cols-2"><button disabled={!selectedSize || !selectedStock} onClick={add} className="flex items-center justify-center gap-2 rounded-full bg-maroon-800 py-3.5 text-xs font-bold uppercase tracking-wider text-white shadow-lg hover:bg-maroon-950 disabled:opacity-50"><ShoppingBag size={16} />Add to cart</button><button disabled={!selectedSize || !selectedStock} onClick={() => { add(); router.push('/cart'); }} className="rounded-full border border-maroon-800 py-3.5 text-xs font-bold uppercase tracking-wider text-maroon-800 disabled:opacity-50">Buy now</button></div></section></div></div></main>;
}
